apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dm-api.fullname" . }}-test-connection"
  labels:
    {{- include "dm-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dm-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
